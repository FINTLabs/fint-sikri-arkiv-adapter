apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mrfylke-no

resources:
  - ../../../base
  - onepassword.yaml

commonLabels:
  app.kubernetes.io/instance: adapter-sikri-arkiv_mrfylke-no
  fintlabs.no/org-id: mrfylke.no

patches:
  
  - target:
      kind: Deployment
      name: adapter-sikri-arkiv
    path: deployment.yaml
