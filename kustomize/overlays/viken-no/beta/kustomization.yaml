apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: viken-no

resources:
  - ../../../base
  - onepassword.yaml

commonLabels:
  app.kubernetes.io/instance: adapter-sikri-arkiv_viken-no
  fintlabs.no/org-id: viken.no

patches:
  
  - target:
      kind: Deployment
      name: adapter-sikri-arkiv
    path: deployment.yaml
